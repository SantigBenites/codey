# Production config
# Owner: platform team
# Last reviewed: 2026-05-09     <-- comment changed (should be IGNORED)
# Promoted to prod after canary

database:
  host: db-prod-2.internal              # value changed
  port: 5432
  pool_size: 50                          # value changed
  ssl: true
  credentials:
      user: app_rw                       # extra indentation (should be IGNORED)
      secret_arn: arn:aws:secretsmanager:eu-west-1:123:secret/db-prod

cache:
  backend: redis
  ttl_seconds: 300
  max_entries: 10000
  compression: gzip                      # NEW key

logging:
  level: DEBUG                           # value changed
  format: json
  destinations:
    - stdout
    - cloudwatch
    - s3                                 # NEW list element

feature_flags:
  new_checkout: true
  legacy_search: false
  # experimental_recs removed            <-- key REMOVED